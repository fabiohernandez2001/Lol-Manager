# Example deployment workflow
# Rename to deploy.yml and configure for your hosting provider

name: Deploy to Production

on:
  push:
    branches: [ main ]
  workflow_dispatch:  # Allow manual triggering

jobs:
  deploy:
    runs-on: ubuntu-latest

    # Only run on main branch
    if: github.ref == 'refs/heads/main'

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    # Example: Deploy to Heroku
    # - name: Deploy to Heroku
    #   uses: akhileshns/heroku-deploy@v3.13.15
    #   with:
    #     heroku_api_key: ${{ secrets.HEROKU_API_KEY }}
    #     heroku_app_name: "your-app-name"
    #     heroku_email: "your-email@example.com"

    # Example: Deploy to AWS Elastic Beanstalk
    # - name: Deploy to AWS EB
    #   uses: einaregilsson/beanstalk-deploy@v21
    #   with:
    #     aws_access_key: ${{ secrets.AWS_ACCESS_KEY_ID }}
    #     aws_secret_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
    #     application_name: lol-manager-backend
    #     environment_name: production
    #     version_label: ${{ github.sha }}
    #     region: us-east-1
    #     deployment_package: deploy.zip

    # Example: Deploy via SSH
    # - name: Deploy via SSH
    #   uses: appleboy/ssh-action@master
    #   with:
    #     host: ${{ secrets.SERVER_HOST }}
    #     username: ${{ secrets.SERVER_USERNAME }}
    #     key: ${{ secrets.SERVER_SSH_KEY }}
    #     script: |
    #       cd /var/www/lol-manager-backend
    #       git pull origin main
    #       source .venv/bin/activate
    #       pip install -r requirements.txt
    #       python manage.py migrate
    #       python manage.py collectstatic --noinput
    #       sudo systemctl restart gunicorn

    # Example: Deploy to Railway
    # - name: Deploy to Railway
    #   uses: bervProject/railway-deploy@main
    #   with:
    #     railway_token: ${{ secrets.RAILWAY_TOKEN }}
    #     service: lol-manager-backend

    - name: Deployment placeholder
      run: |
        echo "Configure your deployment method above"
        echo "Uncomment and customize the relevant section"
